# 刁浩宇 实验室项目 STAR 整理

---

## 项目一：关键产品质量预测软仪表系统

**时间**：2025.09 — 2026.06  
**角色**：项目组长（主导后端架构、数据建模、系统集成）  
**技术栈**：Python Flask + Pyro4 + OPCUA + 时序数据库 + LSTM + LightGBM

---

### S — Situation（情境）

工业现场的生产装置（如精馏塔、反应釜）通过 PLC/DCS 系统采集温度、压力、流量等关键参数，传统做法依赖人工定时巡检与离线化验来判断产品质量，存在三个递进的痛点：

1. **数据采集割裂**：现场设备使用 OPCUA 工业协议，但后端系统无法直接消费——物理设备的数据模型（PLC 寄存器地址、采样频率）与上层应用的信息模型（业务语义如"反应温度""产品纯度"）之间存在**语义鸿沟**，数据即便采集上来也无法直接用于预测。
2. **预测能力缺失**：工艺参数与产品质量之间存在复杂的非线性关系（多变量耦合、时滞效应），人工经验无法准确量化这种映射。一旦工况波动，人工调整存在滞后，可能导致批次产品质量不合格。
3. **系统化要求高**：项目最终目标是上线投产，工厂 7×24 运行，要求系统具备**连续采样、实时预测、高可用**，不能因为一次接口超时或进程崩溃而中断车间生产决策。

### T — Task（任务）

作为项目组长，我负责整体后端架构设计与核心技术攻关，量化目标如下：

| 阶段 | 任务 | 量化目标 |
|------|------|----------|
| 第 1 步 | 打通 OPCUA 物理资产到信息模型的转换 | 采样频率 ≥ **10 次/秒**，数据无损传输 |
| 第 2 步 | 构建多模型融合预测引擎 | 趋势预测准确率 ≥ **93%** |
| 第 3 步 | 完成系统集成与上线投产 | 接口延迟 ≤ **200ms**，系统稳定投产运行 |
| 第 4 步 | 产出可量化的经济效益 | 年经济效益理论测算 ≥ **100 万元** |

### A — Action（行动）

整个工作链路是**自底向上**的：先解决数据怎么采、采什么，再解决怎么算、怎么用，最后解决怎么交付。

**第一步：OPCUA 物理资产到信息模型的转换——打通数据链路**

- 分析现场 PLC/DCS 系统暴露的 OPCUA 节点树结构，识别出与产品质量相关的关键变量（如塔顶温度、进料流量、回流比等），建立**物理寄存器地址 → 业务语义变量**的映射字典。
- 使用 **Pyro4 远程对象通信框架**开发分布式采集代理，部署于边缘计算节点，通过 OPCUA 协议订阅设备实时数据流，以 **10 次/秒**的频率采集并推送至中心服务端。
- 设计统一数据模型的转换层：将原始 OPCUA 节点值（如 `ns=3;s=Temperature.Top`）转换为结构化的 JSON Schema（包含变量名、单位、时间戳、质量戳），实现物理资产的**数字化孪生映射**，使上层预测服务无需关心底层协议细节。
- 采用 **Flask 框架**搭建 RESTful API 服务，提供采样数据查询、预测结果推送、历史趋势回溯等接口。数据写入采用文档型时序数据库，以设备 ID + 时间戳作为复合主键，支持按任意时间窗口范围查询，避免关系型数据库在采样写入场景下的性能瓶颈。

**第二步：多模型融合预测——从数据到决策**

- 数据预处理与特征工程：对原始时序数据进行**滑动窗口标准化**（窗口宽度 = 工艺滞后时间），计算波动率、滚动均值、趋势因子作为衍生特征，解决单变量信息量不足的问题。
- 构建 **LSTM（长短期记忆网络）+ LightGBM 融合模型**：LSTM 捕捉时序数据的长程依赖关系（如温度上升趋势对最终产品纯度的滞后影响），LightGBM 处理静态工艺参数（如原料批次、催化剂类型）与预测量之间的非线性映射，两模型输出加权融合后得到最终预测值。
- 引入 **3σ 统计残差检测**作为模型预测的校验层：计算历史预测残差的标准差 σ，实时预测残差超过 3σ 阈值时触发异常告警，避免模型在未知工况下的过度自信。

**第三步：系统集成与上线交付——从实验室到车间**

- 前端使用 React 构建可视化看板，实时展示采样曲线、预测趋势、异常告警列表，支持按装置/批次/时间段多维筛选。
- 系统部署于工厂内网 Linux 服务器，使用 Supervisor 守护 Flask 进程，配置 Nginx 反向代理与负载均衡，保障 7×24 小时运行。
- 对核心接口（采样写入、预测查询）做**压力测试**，验证在 10 次/秒采集强度 + 多客户端并发查询场景下，接口 P99 延迟稳定在 **200ms 以内**。

### R — Result（结果）

| 指标 | 目标值 | 实际达成 | 验证方式 |
|------|--------|----------|----------|
| 数据采集强度 | ≥10 次/秒 | **10 次/秒**，数据无损 | OPCUA 订阅监控 |
| 接口响应延迟 | ≤200ms | **≤200ms**（P99） | 压力测试报告 |
| 趋势预测准确率 | ≥93% | **96%** | 离线测试集评估 |
| 上线投产 | 稳定运行 | **已上线投产**，持续运行 | 工厂运行日志 |
| 年经济效益 | ≥100 万元 | **理论测算 200 万元/年** | 项目结题报告 |

---

## 项目二：智搭低代码平台 — 大模型驱动的工业 APP 自动建模

**时间**：2024.09 — 2024.09  
**角色**：项目组长（负责方案设计、模块拆解、项目推进、落地交付）  
**技术栈**：大语言模型（LLM）+ Schema 驱动页面生成 + 前端/后端全栈

---

### S — Situation（情境）

工业现场（哈飞钣金车间、总装车间）需要大量定制化的轻量级数据采集与流程管理 APP，例如设备点检记录、工序报工、质检数据录入等。传统开发模式面临三个环环相扣的问题：

1. **供需矛盾**：车间需求多样且变化频繁，但 IT 团队人力有限，一个小型 APP 从需求沟通到上线需要 2-3 周，完全跟不上业务迭代速度。
2. **沟通损耗**：业务人员说不清楚技术需求，开发人员不理解车间场景，双方的**语义鸿沟**导致需求文档反复修改，甚至在开发完成后才发现功能偏差。
3. **复用困难**：不同车间的点检表单、报工流程虽然结构相似但细节不同，每次都需要从零开发，缺乏可配置的抽象层。

核心矛盾的本质是：**如何让非技术人员能够"描述需求即生成 APP"，同时保证生成的 APP 在工业场景下可运行、可配置、可扩展。**

### T — Task（任务）

作为项目组长，我负责整体方案设计与推进，核心目标：

| 阶段 | 任务 | 量化目标 |
|------|------|----------|
| 第 1 步 | 设计大模型驱动的 APP 自动生成方案 | 用户输入自然语言需求 → 自动生成可运行 APP |
| 第 2 步 | 推动团队达成方案共识并拆解开发任务 | 模块可并行开发，项目按期推进 |
| 第 3 步 | 完成平台在工业车间落地验证 | 至少 **2 个车间**实际应用 |

### A — Action（行动）

整体推进经历了**方案设计 → 团队博弈 → 原型验证 → 拆解落地**四个阶段，其中团队协调与技术方案同等重要。

**第一步：技术方案设计——Schema 驱动的页面生成架构**

- 核心思路：用户通过自然语言描述需求（如"我需要一个钣金件首件检验表单，包含工件编号、材料厚度、折弯角度、检验结论四个字段"），大语言模型将其解析为结构化的 **Schema JSON**。
- Schema 定义三要素：① 字段定义（字段名、类型、校验规则、枚举值）→ 映射为表单控件；② 页面布局（分组、排列顺序）→ 映射为页面结构；③ 数据流（提交接口、查询条件、统计维度）→ 映射为后端 CRUD。
- 后端自动生成 RESTful API，前端基于 Schema 渲染组件树，实现**一套 Schema 同时驱动前后端**，避免传统开发中前后端反复对齐接口的低效流程。

**第二步：团队共识建设——从方案争论到统一行动**（关键软技能）

- 初期我从技术角度力推 Schema 方案，但部分同学对低代码平台和 Schema 设计的理解存在差异，对模块边界、页面生成逻辑、数据结构的认知不一致，导致前期讨论周期被拉长，项目进度受到影响。
- 意识到问题后调整策略：① 把技术方案拆成**业务目标、页面结构、模块交互、数据流转**四个独立维度，用具体例子说明"用户输入一句话 → Schema JSON → 页面渲染"的端到端流程，降低理解门槛；② 先做一个**小范围原型**——选一个简单的点检表单场景端到端跑通，让团队看到方案实际运行效果，而非停留在概念辩论；③ 重新梳理成员分工，将前端页面渲染、后端配置管理、模型输出解析等任务拆成**可并行推进的小模块**，并建立周同步机制。
- 团队逐渐统一理解，项目推进恢复正常。

**第三步：落地验证——从原型到车间应用**

- 平台在**哈飞钣金车间**和**总装车间**分别选取典型场景（钣金件首件检验、装配工序报工）进行实地验证。
- 收集车间业务人员的直接反馈：字段校验规则是否贴合实际、操作流程是否简便、数据查询是否方便，据此迭代 Schema 生成规则。
- 最终平台在**两个工业车间实际应用**，验证了"自然语言需求 → 自动生成 APP"这一链路在真实工业场景下的可行性。

### R — Result（结果）

| 指标 | 目标值 | 实际达成 |
|------|--------|----------|
| 需求→APP 生成链路 | 自动生成可运行 APP | **端到端跑通** |
| 工业车间落地 | ≥2 个车间 | **哈飞钣金车间 + 总装车间**实际应用 |
| 团队协作 | 模块并行推进 | 团队完成方案对齐，按期交付 |

---

## 项目三：时序大数据智能监测预警平台

**时间**：2022.09 — 2022.12  
**角色**：项目主导（负责后端架构、数据治理、模型工程、性能优化）  
**技术栈**：Spring Boot + Vue + MyBatis + 时序数据库 + LSTM + LightGBM

---

### S — Situation（情境）

工业设备（电机、泵机、压缩机等）每秒高频采集温度、电压、振动等时序指标，设备运行状态隐藏在**百万级数据点**中。传统监测方式存在三层链式问题：

1. **数据不可用**（源头）：传感器采集的原始数据存在**噪声**（电磁干扰导致的毛刺）、**缺失**（网络抖动导致的时间轴断裂）、**口径不统一**（不同厂商传感器采样频率不同、单位不一致），导致数据直接入库后无法做横向对比和趋势分析。这是后续一切问题的根因。
2. **查询不可达**（中间层）：百万级时序数据的全表扫描查询延迟高达 **500ms+**，运维人员在控制台点击查询后需要等待才能看到趋势图，严重影响故障排查时效。
3. **故障不可预**（结果层）：所有故障都是"事后发现"——设备已经停机或产品质量已经下降后才回溯原因，缺乏**事前预警**能力，损失已经发生。

三层问题的因果链：数据脏 → 查询慢 → 预警难。

### T — Task（任务）

作为项目负责人，我的任务按**从治理到智能**的顺序推进：

| 阶段 | 任务 | 量化目标 |
|------|------|----------|
| 第 1 步 | 数据标准化治理 | 完成百万级时序数据清洗，统一存储口径 |
| 第 2 步 | 多模型融合异常检测 | 异常识别准确率 ≥ **90%**，趋势预测准确率 ≥ **93%** |
| 第 3 步 | 存储与查询性能优化 | 接口查询延迟从 500ms 降至 **≤200ms** |
| 第 4 步 | 7×24 无人值守运行 | 系统持续稳定运行，误报率可控 |

### A — Action（行动）

**第一步：时序数据标准化治理——先把数据变成可用资产**

- **数据清洗**：对原始时序数据做 Z-score 离群点检测，剔除传感器瞬时毛刺（单点突跳 > 4σ 且前后点正常，判定为噪声而非真实异常）；对缺失时间戳做线性插值补全，保证时间轴连续等间隔。
- **时间轴对齐**：不同传感器采样频率不一（如温度 1s/次、振动 10ms/次），采用**降采样 + 聚合窗口**策略——以 1s 为基准窗口，高频信号取窗口内均值/最大值/标准差，低频信号做上采样填充，输出统一频率的多维时序矩阵。
- **数据口径统一**：建立传感器元数据字典（变量名、单位、量程、额定值），所有入库数据经过单位归一化转换（如温度统一为摄氏度、压力统一为 kPa），保证跨设备、跨车间数据可比。

**第二步：多模型融合异常检测——从描述到预判**

- **特征工程**：基于滑动窗口（窗口宽度 = 设备运行周期）计算衍生特征——波动率（标准差/均值）、滚动均值（趋势方向）、趋势因子（一阶差分符号占比），将单一数值转化为多维特征向量，量化设备运行健康状态的动态变化。
- **双模型串联检测**：
  - LSTM 模型：输入历史时序窗口，输出下一时刻的预测值，与真实值比较计算残差。LSTM 通过遗忘门机制学习设备正常运行的时序模式，能够捕捉渐变型异常（如轴承磨损导致的振动缓慢增大）。
  - LightGBM 模型：以特征向量为输入，输出设备当前处于正常/异常状态的概率。LightGBM 基于梯度提升树，对突变型异常（如电机电流瞬间跳变）的响应更灵敏。
  - 两模型输出加权融合：LSTM 残差 + LightGBM 概率分数 → 综合风险评分。
- **双重校验降低误报**：AI 模型输出后，叠加**3σ 统计残差检测**作为第二道闸门——仅当 AI 判定异常且统计残差也超过 3σ 阈值时才触发告警。这避免了单一模型在某些工况下的过敏感误报，将误报率从单模型方案的 12% 降至 **6% 以下**。

**第三步：数据库分层存储与查询优化——让数据查得快**

- **冷热分层存储**：按时间维度将数据分为热数据（近 7 天，存储在 SSD 时序数据库）、温数据（7-30 天，存储在 HDD 但保留索引）、冷数据（30 天以上，压缩归档至对象存储）。热数据占总量约 10% 但承载了 90% 的查询请求。
- **向量化批量计算**：将单点查询改为批量窗口查询，利用时序数据库的向量化引擎一次拉取整个时间窗口的数据，减少网络往返次数（从 N 次 `SELECT` 降至 1 次 `SELECT ... WHERE time BETWEEN`）。
- **多级内存缓存**：前端查询结果在服务端做 LRU 缓存（最近查询过的设备 + 时间段），相同参数重复查询直接返回缓存结果，响应时间降至 **5ms 以内**。
- 经过以上优化，接口平均查询延迟从 **500ms 降至 200ms**，降幅 **60%**。

**第四步：7×24 无人值守运行**

- 预警服务以后台 Daemon 进程运行，定时拉取最新采样数据 → 特征计算 → 模型推理 → 阈值判定，异常结果通过 WebSocket 实时推送至前端看板。
- 配置异常告警规则分级：Warning（轻度偏离，推送消息）→ Critical（显著偏离，邮件 + 短信）→ Emergency（严重偏离，声光报警），避免告警风暴淹没关键信息。

### R — Result（结果）

| 指标 | 目标值 | 实际达成 | 验证方式 |
|------|--------|----------|----------|
| 数据清洗量 | 百万级 | **百万级**时序数据完成治理 | 数据质量报告 |
| 异常识别准确率 | ≥90% | **94%** | 标注测试集 |
| 趋势预测准确率 | ≥93% | **96%** | 标注测试集 |
| 接口查询延迟 | ≤200ms（优化前 500ms） | **200ms**（降幅 60%） | 压力测试 |
| 误报率 | ≤10% | **6%**（双重校验机制） | 告警日志统计 |
| 运行模式 | 7×24 无人值守 | **7×24 稳定运行** | 系统运行日志 |

---

## 三个实验室项目能力映射表

| 专业能力维度 | 软仪表系统 | 智搭低代码 | 时序预警平台 |
|-------------|-----------|-----------|------------|
| **工业协议/边缘计算** | OPCUA、Pyro4 分布式代理、边缘节点部署 | — | — |
| **物理-信息模型转换** | OPCUA 节点树 → 业务语义 JSON 映射 | — | 传感器元数据字典、单位归一化 |
| **时序数据处理** | 文档型时序数据库、滑动窗口标准化 | — | Z-score 去噪、线性插值、降采样对齐 |
| **机器学习/AI** | LSTM + LightGBM 融合、3σ 残差校验 | 大语言模型需求解析 → Schema 生成 | LSTM 渐变检测 + LightGBM 突变检测 + 加权融合 |
| **特征工程** | 波动率、滚动均值、趋势因子 | — | 波动率、滚动均值、趋势因子、一阶差分 |
| **系统架构设计** | Flask RESTful API、前后端分离 | Schema 前后端统一驱动 | 冷热分层存储、LRU 缓存、WebSocket 实时推送 |
| **性能优化** | 压力测试、Supervisor 守护、Nginx 负载均衡 | — | 向量化批量查询、多级缓存、SSD/HDD/归档三级存储 |
| **团队管理/软技能** | 作为项目组长统筹全局 | 方案争论协调、原型验证、模块拆解、项目推进 | 项目主导 |
| **业务价值** | 上线投产，年效益 200 万元 | 两车间实际应用 | 94% 异常识别、7×24 无人值守 |
