# 刁浩宇 实习经历 STAR 整理

---

## 实习一：京东科技 — 金融拉新应用后端开发

**时间**：2025.06 — 2025.09  
**岗位**：平台研发部 后端开发实习生  
**技术栈**：Java + Spring Boot + MyBatis + Redis + MySQL

---

### S — Situation（情境）

京东金融拉新业务线通过拼团抽奖、即时抽奖、电子木鱼小游戏等营销活动推动用户增长。活动上线后，系统面临的挑战并非同时出现，而是随流量爬升**逐层暴露**：

1. **第一阶段——查询瓶颈**：活动预热期 UV 攀升后，数据库慢查询日志开始出现大量全表扫描 SQL，核心抽奖接口 P99 延迟从 50ms 飙升至 800ms+。根因在于 MyBatis 映射的关联查询缺少索引覆盖，且热点数据（活动配置、用户参与状态）每次请求都穿透到 MySQL，未利用缓存减少磁盘 I/O。
2. **第二阶段——并发风险**：活动正式上线后，瞬时并发突破 3000，数据库连接池（HikariCP）出现等待队列堆积。更深层的问题是：优惠券发放与用户状态变更属于**多步非原子操作**，若中间步骤失败未回滚，会出现超发（重复发放）或少发（扣减了资格但未发券）的数据不一致问题。

### T — Task（任务）

基于以上问题暴露的时间线，我的任务按**优先级逐层递进**：

| 优先级 | 任务 | 量化目标 |
|--------|------|----------|
| P0 | 解决慢查询与缓存缺失，恢复接口可用性 | 核心接口 P99 响应时间 ≤ **200ms** |
| P1 | 保障并发场景数据一致性，防止超发/少发 | 峰值支持 **5000+ 用户并发**，资金损失 = **0** |

### A — Action（行动）

整个解决过程有明确的**先后依赖关系**——先止血、再加固、后防护、最后闭环。

**第一步：SQL 优化 + 缓存层搭建（止血 → 恢复性能）**

- 通过 `EXPLAIN` 分析慢查询执行计划，发现缺少复合索引导致回表查询，补建 `(activity_id, status, create_time)` 联合索引，将全表扫描转为索引覆盖，单次查询从 120ms 降至 5ms。
- 引入 Redis 作为缓存层，采用 **Cache-Aside 模式**：读请求先查 Redis，未命中再查 MySQL 并回填缓存；写请求先更新 MySQL，再异步删除 Redis 对应 Key，避免缓存与数据库数据不一致。
- 对活动配置、用户参与资格等读多写少的热点数据设置 TTL 过期策略，防止缓存内存膨胀。缓存命中率从 0 提升至 **92%**，MySQL 读 QPS 下降 **70%**。

**第二步：并发控制与事务一致性（加固 → 防数据错乱）**

- 优惠券发放链路拆分为 3 步：校验资格 → 扣减库存 → 写入发放记录。三个操作封装在同一 `@Transactional` 事务边界内，利用 MySQL InnoDB 的 **ACID 特性**保证原子性：任一步失败则整体回滚，避免"扣了库存没发券"的半状态。
- 对扣减库存操作采用 **行级锁（SELECT ... FOR UPDATE）**，防止并发扣减导致库存超卖。同时配置 HikariCP 连接池最大连接数从默认 10 提升至 30，配合连接泄漏检测（`leakDetectionThreshold`），消除连接等待堆积。
- 接口层引入 **滑动窗口限流算法**（基于 Redis ZSET），对同一用户同一活动限制每秒请求次数，超过阈值直接返回 429，避免恶意高频调用打垮下游。

### R — Result（结果）

| 指标 | 目标值 | 实际达成 | 验证方式 |
|------|--------|----------|----------|
| 核心接口 P99 响应时间 | ≤200ms | ≤200ms（从 800ms+ 优化） | Grafana 监控面板 |
| 峰值并发支持 | ≥5000 用户 | 5000+，系统无宕机 | 压力测试报告 |
| 缓存命中率 | ≥85% | **92%** | Redis INFO stats |

---

## 实习二：字节跳动 — 国际化 CRM 税票平台重构

**时间**：2026.05 — 至今  
**岗位**：国际化 CRM 部门 后端开发实习生  
**技术栈**：Go + Java (Spring Boot) + Python + TypeScript + Redis + MySQL

---

### S — Situation（情境）

字节跳动国际化 CRM 税票平台为 **10+ 业务方**提供跨境税务开票能力，业务方包括电商、广告、直播等不同实体。系统经过多轮迭代后暴露出三层次问题，且层次之间存在**因果关系**：

1. **数据层——模型腐化**（根因）：历史开发中各业务方独立建表存储税务信息，不同表的字段命名、枚举值、关联关系不一致。例如同一企业在三张表中分别用 `tax_id`、`ein`、`company_tax_no` 表示税号，导致跨业务方查询需做多表 JOIN 并人工对齐字段语义。这是后续接口臃肿和接入效率低的**根本原因**。
2. **服务层——接口臃肿**（后果 1）：因为底层数据模型不统一，每个接口需要兼容多套字段映射逻辑，导致接口方法体膨胀至 300+ 行，圈复杂度超过 20。单接口 QPS 瓶颈仅 1k req/s，峰值时段 P99 延迟飙升至 2s，直接拖慢开票时效。
3. **运维层——人工依赖**（后果 2）：模型和接口的混乱使得业务方无法自助完成税务信息查询与开票，每次接入或异常排查都依赖开发人员手工 SQL 查库、手动拼接字段，单次接入耗时 3-5 天。

三层问题形成**恶性循环**：模型腐化 → 接口难维护 → 自动化无法落地 → 人工依赖加剧 → 新需求继续堆砌 → 模型进一步腐化。

### T — Task（任务）

面对以上因果链，我的任务按**从根到梢**的解决顺序展开：

| 阶段 | 任务 | 量化目标 |
|------|------|----------|
| 第 1 步：建模 | 梳理历史库表关系，提出统一数据模型 | 消除字段冗余，抽象出唯一业务实体标识 |
| 第 2 步：重构 | 基于新模型重构后端接口并迁移数据 | 重构 **23 个接口**，迁移 **10+ 业务方**，QPS ≥ **4k req/s** |
| 第 3 步：提效 | 构建自动化 Skill/Agent，降低人工依赖 | 新业务方接入 ≤ **1 天**，人工工作量降低 **50%+** |
| 第 4 步：质量 | 接入白盒测试体系，保障重构不引入回归问题 | 核心接口测试覆盖率 ≥ **80%** |

### A — Action（行动）

这四步存在严格的**先后依赖关系**——不建模就没法重构，不重构就没法自动化，不测试就不能保证质量。

**第一步：存量数据建模——用 DDD 思维梳理业务（先看清再动手）**

- 逐表导出历史库表结构（DDL），梳理 10+ 业务方的税务信息表字段命名、数据类型、外键关系，绘制 **ER 图**和**数据血缘图**，标记冗余字段与循环依赖。
- 发现核心问题：不同业务方对"企业"的建模不一致——有的用 `enterprise_id`，有的用 `company_code`，有的直接用业务方的 `account_id` 充当企业标识。这种多义性是上层所有混乱的根因。
- 提出 **`company` 统一聚合根**（借鉴 DDD 领域驱动设计思想）：将税务信息建模为以 company 为核心的聚合，其下包含 tax_profile（税务档案）、invoice_record（开票记录）、tax_exemption（免税资质）等值对象。消除跨表冗余字段 15+ 个，将原本分散的 12 张表收敛为 4 张核心表。

**第二步：接口重构——分层解耦 + 语言栈切换（从建模到落地）**

- 基于新数据模型，重新设计 RESTful API 规范，统一入参/出参 Schema 与业务错误码枚举（0 → 成功，1xxx → 参数校验错误，2xxx → 业务逻辑错误，3xxx → 系统异常）。
- 接口实现遵循 **Controller → Service → Repository 三层解耦**：Controller 层仅做参数校验与 DTO 转换，Service 层封装业务逻辑（如开票前置校验、税率计算），Repository 层通过 MyBatis/Go ORM 与数据库交互。每个接口方法体从 300+ 行缩减至平均 **60 行以内**，圈复杂度降至 **8 以下**。
- 查询密集型接口使用 Go（goroutine 高并发优势），业务逻辑密集型接口保留 Java Spring，两者通过公司内部 RPC 框架通信。重构后批量查询接口 QPS 从 1k 提升至 **4k — 5k req/s**。

**第三步：自动化 Skill / Agent 开发——让业务方自助（从手工到自动）**

- 基于 Python（FastAPI）开发对内 **Skill**：封装税务信息查询、开票申请、资质核验等高频操作为标准化 API，业务方通过内部平台一键调用，无需了解底层表结构。
- 基于 Python + TypeScript 开发对外 **Tax Agent**：对接外部税务系统（如各国税局的电子发票接口），实现开票请求自动路由、失败自动重试（指数退避）、异常自动告警。Agent 运行于公司容器化平台，支持按业务方维度独立扩缩容。
- 架构设计时考虑**关注点分离**：Skill 面向内部使用体验优化（易用性优先），Agent 面向外部系统对接可靠性（容错优先），两者共享底层 Service 层，避免逻辑重复。

**第四步：白盒测试体系——保障重构质量（从能跑到可靠）**

- 基于 **aico-test 框架**编写白盒单元测试：对每个核心 Service 方法，按正常路径、参数边界（null / 空字符串 / 超长字符串 / 特殊字符）、异常路径（数据库超时 / 外部服务不可用）三类场景分别设计测试用例。
- 编写集成测试覆盖接口级联调用链路（如：税务信息查询 → 开票资格校验 → 开票 → 写入记录），验证跨服务数据一致性。
- 将测试接入 CI 流水线，每次代码提交自动触发全量测试，测试不通过则阻断合并。

### R — Result（结果）

| 指标 | 目标值 | 实际达成 | 验证方式 |
|------|--------|----------|----------|
| 模型简化 | — | 12 张表收敛为 4 张，消除冗余字段 15+ | DDL diff |
| 接口重构数量 | 23 个 | **23 个接口全部重构完成** | 代码仓库统计 |
| 业务方迁移 | 10+ 业务方 | **10+ 业务方**数据平滑迁移 | 迁移对账脚本 |
| 接口 QPS | ≥4k req/s | **4k — 5k req/s** | 压测报告 |
| 接口代码复杂度 | — | 方法体 ≤60 行，圈复杂度 ≤8 | SonarQube |
| 新业务接入周期 | ≤1 天 | **≤1 天** | 接入记录 |
| 人工对接工作量 | 减少 ≥50% | **减少 50%+** | Skill/Agent 调用统计 |
| 核心接口测试覆盖率 | ≥80% | **80%+** | CI 覆盖率报告 |

---

## 两项实习能力映射表

| 专业能力维度 | 京东科技体现 | 字节跳动体现 |
|-------------|-------------|-------------|
| **数据库** | SQL 执行计划分析、联合索引设计、行级锁、事务 ACID | ER 图建模、DDD 聚合根设计、数据血缘梳理、库表收敛 |
| **缓存** | Cache-Aside 模式、TTL 策略、缓存命中率监控 | 批量查询缓存优化 |
| **并发控制** | 滑动窗口限流、HikariCP 连接池调优 | Go goroutine 并发模型、指数退避重试 |
| **系统设计** | Spring MVC 分层架构、灰度发布 | Controller-Service-Repository 分层、RESTful API 规范、关注点分离 |
| **代码质量** | 单元测试、Prometheus + Grafana 监控 | 白盒测试（正常/边界/异常路径）、CI 自动化测试门禁、SonarQube 指标 |
| **方法论** | 问题分层诊断（查询瓶颈→并发风险→黑产攻击） | DDD 领域建模、因果链分析（模型腐化→接口臃肿→人工依赖）、重构优先于自动化 |
| **跨语言能力** | Java + Spring Boot | Go + Java + Python + TypeScript |
